/**
 * ============================================================================
 * GLES20.java 详细注释版本
 * OpenGL ES 2.0 API 参考指南
 * ============================================================================
 *
 * 本文件是对 Android SDK 中 GLES20.java 的详细注释版本
 * 原文位置: /Users/liguang/Library/Android/sdk/sources/android-36.1/android/opengl/GLES20.java
 *
 * ============================================================================
 * 文件概述
 * ============================================================================
 *
 * GLES20 是 Android 系统提供的 OpenGL ES 2.0 Java 绑定类。
 * 它提供了约 500 个常量和 100+ 个 native 方法，
 * 用于在 Android 应用中调用 GPU 进行图形渲染。
 *
 * OpenGL ES (Open Graphics Library for Embedded Systems) 是专为:
 * - 移动设备（手机、平板）
 * - 嵌入式系统
 * - 游戏主机
 * 设计的图形 API。
 *
 * ============================================================================
 * 设计架构分析
 * ============================================================================
 *
 * 1. 为什么使用 static final int 而不是枚举?
 * ---------------------------------------------------------------------------
 *
 *    OpenGL 常量数量庞大（约 500 个），如果使用 Java 枚举:
 *
 *    枚举的内存开销:
 *    - 每个枚举值都是一个对象，需要堆内存分配
 *    - 枚举类加载时需要初始化所有实例
 *    - 比较时需要对象身份检查（identity comparison）
 *
 *    static final int 的优势:
 *    - 编译时常量，没有运行时内存分配
 *    - JIT 编译器会对 switch-case 优化成跳转表
 *    - 与 C 语言的 OpenGL 规范完全兼容
 *
 *    历史原因:
 *    - OpenGL 最初用 C 编写，习惯使用整数常量
 *    - Android 需要与 native 驱动层保持 ABI 兼容
 *    - 保持与 Khronos 官方规范的一致性
 *
 * 2. 为什么使用 public static native 方法?
 * ---------------------------------------------------------------------------
 *
 *    Java/Kotlin 代码调用流程:
 *
 *    ┌─────────────────┐
 *    │  Java/Kotlin    │
 *    │    代码         │
 *    └────────┬────────┘
 *             │ glDrawArrays(...)
 *             ▼
 *    ┌─────────────────┐
 *    │   JNI 层        │
 *    │ (C/C++ 绑定)    │
 *    └────────┬────────┘
 *             │ 转发调用
 *             ▼
 *    ┌─────────────────┐
 *    │  GPU 驱动       │
 *    │ (C/C++)         │
 *    └────────┬────────┘
 *             │ GPU 命令
 *             ▼
 *    ┌─────────────────┐
 *    │   GPU 硬件       │
 *    └─────────────────┘
 *
 *    为什么必须用 native:
 *    - GPU 驱动是 C/C++ 编写
 *    - GPU 通过 PCIe/AGP 总线通信，需要特殊处理
 *    - 直接内存访问 (DMA) 需要 native 代码
 *    - 性能关键路径不能有 Java 间接调用开销
 *
 * 3. 为什么这么多常量?
 * ---------------------------------------------------------------------------
 *
 *    OpenGL 是状态机 (State Machine)，需要常量描述状态:
 *
 *    状态分类:
 *    - 图元类型 (TRIANGLES, LINES, POINTS)
 *    - 缓冲区目标 (ARRAY_BUFFER, ELEMENT_ARRAY_BUFFER)
 *    - 启用特性 (DEPTH_TEST, BLEND, CULL_FACE)
 *    - 纹理参数 (TEXTURE_MIN_FILTER, TEXTURE_WRAP_S)
 *    - 比较函数 (LESS, GREATER, EQUAL)
 *    - 错误代码 (INVALID_ENUM, INVALID_VALUE)
 *
 *    常量命名约定:
 *    - GL_ 前缀表示 OpenGL 命名空间
 *    - _BIT 后缀表示清除/启用标志
 *    - _MAX 后缀表示查询最大值
 *
 * ============================================================================
 * 渲染管线概述
 * ============================================================================
 *
 * OpenGL ES 2.0 可编程渲染管线:
 *
 * 应用程序阶段:
 * ┌─────────────────────────────────────────┐
 * │  CPU: 准备顶点数据、设置状态、上传纹理  │
 * └────────────────┬────────────────────────┘
 *                  │
 *                  ▼
 *
 * 顶点处理阶段 (Vertex Processing):
 * ┌─────────────────────────────────────────┐
 * │  顶点着色器 (Vertex Shader)              │
 * │  - 变换顶点位置                          │
 * │  - 计算顶点属性                          │
 * │  - 输出到裁剪空间                        │
 * └────────────────┬────────────────────────┘
 *                  │
 *                  ▼
 *
 * 几何处理阶段 (Geometry Processing):
 * ┌─────────────────────────────────────────┐
 * │  - 图元组装 (Primitive Assembly)        │
 * │  - 裁剪 (Clipping)                      │
 * │  - 透视分割 (Perspective Division)      │
 * │  - 视口变换 (Viewport Transform)         │
 * └────────────────┬────────────────────────┘
 *                  │
 *                  ▼
 *
 * 光栅化阶段 (Rasterization):
 * ┌─────────────────────────────────────────┐
 * │  - 将图元转换为片段 (Fragment)          │
 * │  - 计算片段属性（屏幕坐标、深度等）      │
 * └────────────────┬────────────────────────┘
 *                  │
 *                  ▼
 *
 * 片段处理阶段 (Fragment Processing):
 * ┌─────────────────────────────────────────┐
 * │  片段着色器 (Fragment Shader)            │
 * │  - 纹理采样                              │
 * │  - 光照计算                              │
 * │  - 颜色计算                              │
 * └────────────────┬────────────────────────┘
 *                  │
 *                  ▼
 *
 * 输出合并阶段 (Output Merging):
 * ┌─────────────────────────────────────────┐
 * │  - 深度测试                            │
 * │  - 模板测试                            │
 * │  - 混合 (Blending)                     │
 * │  - 写入帧缓冲区                        │
 * └─────────────────────────────────────────┘
 *
 * ============================================================================
 * 核心概念详解
 * ============================================================================
 *
 * 1. 顶点缓冲区对象 (VBO - Vertex Buffer Object)
 * ---------------------------------------------------------------------------
 *
 *    传统方式: 每次绘制从 CPU 内存上传顶点数据到 GPU
 *    VBO 方式: 顶点数据预先存储在 GPU 内存，直接读取
 *
 *    性能优势:
 *    - 避免每次绘制时的数据传输
 *    - GPU 可以并行处理顶点数据
 *    - 支持多线程异步准备数据
 *
 *    使用模式:
 *    - GL_STATIC_DRAW: 数据从不改变（最优性能）
 *    - GL_DYNAMIC_DRAW: 数据偶尔改变
 *    - GL_STREAM_DRAW: 数据频繁改变
 *
 *    代码示例:
 *    ┌──────────────────────────────────────┐
 *    │  // 创建缓冲区                        │
 *    │  val buffers = IntArray(1)           │
 *    │  GLES20.glGenBuffers(1, buffers, 0)  │
 *    │  val vbo = buffers[0]               │
 *    │                                      │
 *    │  // 绑定并上传数据                    │
 *    │  GLES20.glBindBuffer(                │
 *    │      GLES20.GL_ARRAY_BUFFER, vbo)    │
 *    │  GLES20.glBufferData(                │
 *    │      GLES20.GL_ARRAY_BUFFER,         │
 *    │      size, data,                      │
 *    │      GLES20.GL_STATIC_DRAW)          │
 *    └──────────────────────────────────────┘
 *
 * 2. 着色器 (Shader)
 * ---------------------------------------------------------------------------
 *
 *    顶点着色器 (Vertex Shader):
 *    - 每个顶点执行一次
 *    - 负责顶点变换 MVP (Model-View-Projection)
 *    - 输出裁剪空间坐标
 *
 *    片段着色器 (Fragment Shader):
 *    - 每个像素执行一次
 *    - 负责最终颜色计算
 *    - 可以进行纹理采样、光照计算等
 *
 *    GLSL ES 语法特点:
 *    - 类似 C 的语法
 *    - 内置类型: vec2, vec3, vec4, mat4, sampler2D
 *    - 限定符: attribute (顶点), uniform (全局), varying (插值)
 *
 * 3. uniform 变量
 * ---------------------------------------------------------------------------
 *
 *    uniform vs attribute:
 *    ┌──────────────┬─────────────────┬────────────────┐
 *    │    特性      │    attribute    │    uniform     │
 *    ├──────────────┼─────────────────┼────────────────┤
 *    │ 更新频率    │ 每个顶点更新    │ 每帧更新一次   │
 *    │ 数据流向    │ CPU → GPU       │ CPU → GPU      │
 *    │ 存储位置    │ 顶点缓冲区      │ GPU 常量内存   │
 *    │ 示例        │ 顶点位置、颜色  │ 变换矩阵、时间 │
 *    └──────────────┴─────────────────┴────────────────┘
 *
 * 4. 深度测试 (Depth Testing)
 * ---------------------------------------------------------------------------
 *
 *    作用: 解决物体遮挡问题，决定哪些像素可见
 *
 *    原理:
 *    - 每个像素存储一个深度值（0.0-1.0）
 *    - 新像素与已存像素比较
 *    - 根据比较函数决定通过或丢弃
 *
 *    比较函数:
 *    - GL_LESS: 新深度 < 存储深度 → 通过（最常用）
 *    - GL_LEQUAL: 新深度 <= 存储深度 → 通过
 *    - GL_GREATER: 新深度 > 存储深度 → 通过
 *    - GL_ALWAYS: 总是通过
 *    - GL_NEVER: 从不通过
 *
 * 5. 混合 (Blending)
 * ---------------------------------------------------------------------------
 *
 *    作用: 实现透明、半透明效果
 *
 *    混合公式:
 *    ┌─────────────────────────────────────┐
 *    │ result = src_color × src_factor +   │
 *    │         dst_color × dst_factor       │
 *    └─────────────────────────────────────┘
 *
 *    最常用组合:
 *    ┌─────────────────────────────────────┐
 *    │ GL_SRC_ALPHA,       // 源 alpha     │
 *    │ GL_ONE_MINUS_SRC_ALPHA              │
 *    │ // 透明物体: 源×alpha + 目标×(1-alpha)│
 *    └─────────────────────────────────────┘
 *
 *    应用场景:
 *    - 半透明 UI 元素
 *    - 粒子系统
 *    - 发光效果 (GL_ONE, GL_ONE)
 *    - 软混合 (GL_SRC_ALPHA, GL_ONE)
 *
 * 6. 纹理 (Texture)
 * ---------------------------------------------------------------------------
 *
 *    纹理坐标系统:
 *    ┌─────────────────────────┐
 *    │ (0,1)          (1,1)   │
 *    │    ┌───────────┐        │
 *    │    │           │        │
 *    │    │   纹理    │        │
 *    │    │           │        │
 *    │    └───────────┘        │
 *    │ (0,0)          (1,0)   │
 *    └─────────────────────────┘
 *
 *    环绕模式 (Wrap Mode):
 *    - GL_REPEAT: 重复（适合地形）
 *    - GL_CLAMP_TO_EDGE: 钳制到边缘（适合 UI）
 *    - GL_MIRRORED_REPEAT: 镜像重复（适合无缝）
 *
 *    过滤模式 (Filter Mode):
 *    - GL_NEAREST: 最近邻（像素风）
 *    - GL_LINEAR: 双线性插值（平滑）
 *    - MIPMAP: 多级渐远（减少远处闪烁）
 *
 * ============================================================================
 * 最佳实践
 * ============================================================================
 *
 * 1. 错误处理模式
 * ---------------------------------------------------------------------------
 *
 *    OpenGL 操作是异步的，错误不会抛出异常，而是累积到错误队列
 *
 *    正确做法:
 *    ┌──────────────────────────────────────┐
 *    │  // 绘制前检查                         │
 *    │  GLES20.glClear(GLES20.GL_ERROR)     │
 *    │                                      │
 *    │  // 执行 OpenGL 操作                   │
 *    │  GLES20.glDrawArrays(...)            │
 *    │                                      │
 *    │  // 检查错误                           │
 *    │  val error = GLES20.glGetError()    │
 *    │  if (error != GLES20.GL_NO_ERROR) {  │
 *    │      Log.e("OpenGL", "Error: $error") │
 *    │  }                                   │
 *    └──────────────────────────────────────┘
 *
 *    注意: glGetError() 会清除错误状态
 *          所以应该每帧只调用一次
 *
 * 2. 资源管理
 * ---------------------------------------------------------------------------
 *
 *    GPU 资源是有限的，必须显式释放:
 *
 *    ┌──────────────────────────────────────┐
 *    │  // 删除着色器                         │
 *    │  GLES20.glDeleteShader(shader)       │
 *    │                                      │
 *    │  // 删除程序                           │
 *    │  GLES20.glDeleteProgram(program)     │
 *    │                                      │
 *    │  // 删除缓冲区                         │
 *    │  GLES20.glDeleteBuffers(1, buffers, 0)│
 *    │                                      │
 *    │  // 删除纹理                           │
 *    │  GLES20.glDeleteTextures(1, textures,0)│
 *    └──────────────────────────────────────┘
 *
 *    最佳时机:
 *    - Activity onDestroy() 时清理
 *    - GLSurfaceView onSurfaceDestroyed() 时清理
 *    - 切换渲染器时清理旧资源
 *
 * 3. 性能优化
 * ---------------------------------------------------------------------------
 *
 *    减少状态切换:
 *    ┌──────────────────────────────────────┐
 *    │  // 不推荐: 频繁切换程序               │
 *    │  for (object in objects) {            │
 *    │      GLES20.glUseProgram(prog1)       │
 *    │      draw(object1)                   │
 *    │      GLES20.glUseProgram(prog2)       │
 *    │      draw(object2)                   │
 *    │  }                                   │
 *    │                                      │
 *    │  // 推荐: 按程序分组                   │
 *    │  GLES20.glUseProgram(prog1)           │
 *    │  for (obj in objects1) draw(obj)     │
 *    │  GLES20.glUseProgram(prog2)           │
 *    │  for (obj in objects2) draw(obj)     │
 *    └──────────────────────────────────────┘
 *
 *    批量处理顶点:
 *    ┌──────────────────────────────────────┐
 *    │  // 不推荐: 多次绘制调用               │
 *    │  for (i in 0..99) {                  │
 *    │      drawTriangle(triangle[i])       │
 *    │  }                                   │
 *    │                                      │
 *    │  // 推荐: 合并顶点数据                 │
 *    │  drawTriangles(allTriangles)         │
 *    └──────────────────────────────────────┘
 *
 *    合理使用 STATIC_DRAW:
 *    - 静态物体（建筑物、地形）用 STATIC
 *    - 动态物体（粒子）用 DYNAMIC
 *    - 不要过度使用 DYNAMIC（GPU 优化受限）
 *
 * 4. 线程安全
 * ---------------------------------------------------------------------------
 *
 *    OpenGL Context 只能在创建它的线程使用:
 *
 *    GLSurfaceView 的线程模型:
 *    ┌──────────────────────────────────────┐
 *    │  主线程 (UI 线程)                     │
 *    │  - 创建 GLSurfaceView                 │
 *    │  - 设置 Renderer                      │
 *    │  - 处理 UI 事件                       │
 *    │         │                             │
 *    │         ▼                             │
 *    │  GL 渲染线程                          │
 *    │  - 执行 Renderer.onDrawFrame()        │
 *    │  - 所有 OpenGL 调用                   │
 *    │         │                             │
 *    │         ▼                             │
 *    │  事件队列 (SurfaceView 内部)          │
 *    │  - 传递 UI 事件到渲染线程             │
 *    └──────────────────────────────────────┘
 *
 *    重要规则:
 *    - 不要在主线程调用 OpenGL 函数
 *    - 不要在渲染线程创建 UI 对象
 *    - 使用 queueEvent() 从主线程向渲染线程传递数据
 *
 * ============================================================================
 * 常用代码模板
 * ============================================================================
 *
 * 1. 完整的着色器编译+链接流程
 * ---------------------------------------------------------------------------
 * ┌────────────────────────────────────────────────────────────────────────┐
 * │                                                                          │
 * │  // 顶点着色器代码                                                       │
 * │  val vertexShaderCode = """                                              │
 * │      attribute vec4 vPosition;                                           │
 * │      void main() {                                                       │
 * │          gl_Position = vPosition;                                       │
 * │      }                                                                   │
 * │  """                                                                    │
 * │                                                                          │
 * │  // 片段着色器代码                                                       │
 * │  val fragmentShaderCode = """                                            │
 * │      precision mediump float;                                            │
 * │      void main() {                                                       │
 * │          gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);                       │
 * │      }                                                                   │
 * │  """                                                                    │
 * │                                                                          │
 * │  // 1. 创建着色器                                                        │
 * │  val vertexShader = GLES20.glCreateShader(GLES20.GL_VERTEX_SHADER)     │
 * │  val fragmentShader = GLES20.glCreateShader(GLES20.GL_FRAGMENT_SHADER)│
 * │                                                                          │
 * │  // 2. 加载源代码                                                        │
 * │  GLES20.glShaderSource(vertexShader, vertexShaderCode)                 │
 * │  GLES20.glShaderSource(fragmentShader, fragmentShaderCode)             │
 * │                                                                          │
 * │  // 3. 编译着色器                                                        │
 * │  GLES20.glCompileShader(vertexShader)                                   │
 * │  GLES20.glCompileShader(fragmentShader)                                 │
 * │                                                                          │
 * │  // 4. 检查编译状态                                                      │
 * │  val vertexStatus = IntArray(1)                                         │
 * │  GLES20.glGetShaderiv(vertexShader, GLES20.GL_COMPILE_STATUS,          │
 * │                        vertexStatus, 0)                                  │
 * │  if (vertexStatus[0] != GLES20.GL_TRUE) {                              │
 * │      Log.e("OpenGL", "Vertex shader compile error: " +                 │
 * │             GLES20.glGetShaderInfoLog(vertexShader))                    │
 * │  }                                                                      │
 * │                                                                          │
 * │  // 5. 创建程序                                                         │
 * │  val program = GLES20.glCreateProgram()                                │
 * │                                                                          │
 * │  // 6. Attach 着色器                                                     │
 * │  GLES20.glAttachShader(program, vertexShader)                          │
 * │  GLES20.glAttachShader(program, fragmentShader)                        │
 * │                                                                          │
 * │  // 7. 链接程序                                                         │
 * │  GLES20.glLinkProgram(program)                                          │
 * │                                                                          │
 * │  // 8. 检查链接状态                                                      │
 * │  val linkStatus = IntArray(1)                                          │
 * │  GLES20.glGetProgramiv(program, GLES20.GL_LINK_STATUS,                 │
 * │                         linkStatus, 0)                                  │
 * │  if (linkStatus[0] != GLES20.GL_TRUE) {                                 │
 * │      Log.e("OpenGL", "Program link error: " +                          │
 * │             GLES20.glGetProgramInfoLog(program))                        │
 * │  }                                                                      │
 * │                                                                          │
 * │  // 9. 获取 attribute/uniform 位置（缓存备用）                          │
 * │  val positionHandle = GLES20.glGetAttribLocation(program, "vPosition") │
 * │                                                                          │
 * └────────────────────────────────────────────────────────────────────────┘
 *
 * 2. 顶点缓冲区创建+绑定+上传流程
 * ---------------------------------------------------------------------------
 * ┌────────────────────────────────────────────────────────────────────────┐
 * │                                                                          │
 * │  // 顶点数据（三角形）                                                  │
 * │  val vertices = floatArrayOf(                                           │
 * │      0.0f, 0.5f, 0.0f,    // 顶点 1                                    │
 * │     -0.5f, -0.5f, 0.0f,    // 顶点 2                                    │
 * │      0.5f, -0.5f, 0.0f     // 顶点 3                                    │
 * │  )                                                                      │
 * │                                                                          │
 * │  // 1. 生成缓冲区 ID                                                     │
 * │  val buffers = IntArray(1)                                             │
 * │  GLES20.glGenBuffers(1, buffers, 0)                                    │
 * │  val vbo = buffers[0]                                                  │
 * │                                                                          │
 * │  // 2. 绑定缓冲区                                                        │
 * │  GLES20.glBindBuffer(GLES20.GL_ARRAY_BUFFER, vbo)                      │
 * │                                                                          │
 * │  // 3. 创建 FloatBuffer 并填充数据                                      │
 * │  val vertexBuffer = ByteBuffer.allocateDirect(vertices.size * 4)       │
 * │      .order(ByteOrder.nativeOrder())                                   │
 * │      .asFloatBuffer()                                                   │
 * │  vertexBuffer.put(vertices)                                             │
 * │  vertexBuffer.position(0)                                               │
 * │                                                                          │
 * │  // 4. 上传到 GPU                                                        │
 * │  GLES20.glBufferData(                                                   │
 * │      GLES20.GL_ARRAY_BUFFER,                                            │
 * │      vertices.size * 4,                                                 │
 * │      vertexBuffer,                                                      │
 * │      GLES20.GL_STATIC_DRAW)                                             │
 * │                                                                          │
 * │  // 5. 解绑（可选，但推荐）                                             │
 * │  GLES20.glBindBuffer(GLES20.GL_ARRAY_BUFFER, 0)                        │
 * │                                                                          │
 * └────────────────────────────────────────────────────────────────────────┘
 *
 * 3. 每帧绘制流程
 * ---------------------------------------------------------------------------
 * ┌────────────────────────────────────────────────────────────────────────┐
 * │                                                                          │
 * │  override fun onDrawFrame(gl: GL10?) {                                  │
 * │                                                                          │
 * │      // 1. 清除旧帧数据                                                  │
 * │      GLES20.glClear(GLES20.GL_COLOR_BUFFER_BIT or                       │
 * │                      GLES20.GL_DEPTH_BUFFER_BIT)                        │
 * │                                                                          │
 * │      // 2. 使用程序                                                      │
 * │      GLES20.glUseProgram(programId)                                     │
 * │                                                                          │
 * │      // 3. 绑定顶点缓冲区                                                │
 * │      GLES20.glBindBuffer(GLES20.GL_ARRAY_BUFFER, vbo)                  │
 * │                                                                          │
 * │      // 4. 设置顶点属性                                                  │
 * │      GLES20.glEnableVertexAttribArray(positionHandle)                   │
 * │      GLES20.glVertexAttribPointer(                                      │
 * │          positionHandle, 3, GLES20.GL_FLOAT,                           │
 * │          false, 0, 0)                                                  │
 * │                                                                          │
 * │      // 5. 设置 uniform（如有）                                          │
 * │      GLES20.glUniformMatrix4fv(mvpMatrixHandle, 1, false,              │
 * │                               mvpMatrix, 0)                              │
 * │                                                                          │
 * │      // 6. 绘制                                                          │
 * │      GLES20.glDrawArrays(GLES20.GL_TRIANGLES, 0, 3)                    │
 * │                                                                          │
 * │      // 7. 清理状态                                                      │
 * │      GLES20.glDisableVertexAttribArray(positionHandle)                  │
 * │      GLES20.glBindBuffer(GLES20.GL_ARRAY_BUFFER, 0)                    │
 * │  }                                                                      │
 * │                                                                          │
 * └────────────────────────────────────────────────────────────────────────┘
 *
 * ============================================================================
 * 常见错误与解决方案
 * ============================================================================
 *
 * 1. 编译错误: "syntax error"
 * ---------------------------------------------------------------------------
 *    原因: GLSL 语法错误
 *    检查: 分号、花括号、变量名拼写
 *
 * 2. 链接错误: "link program error"
 * ---------------------------------------------------------------------------
 *    原因: 顶点/片段着色器变量不匹配
 *    检查: attribute/uniform 名称完全一致
 *
 * 3. 黑屏（什么都不显示）
 * ---------------------------------------------------------------------------
 *    可能原因:
 *    - 顶点坐标全在视锥体外
 *    - 深度测试过于严格
 *    - 背面剔除方向错误
 *    检查: 先简化顶点数据，禁用深度测试和背面剔除
 *
 * 4. 纹理不显示
 * ---------------------------------------------------------------------------
 *    可能原因:
 *    - 纹理坐标超出范围 [0,1]
 *    - 纹理未正确绑定
 *    - 纹理参数设置错误
 *    检查: 使用 GL_CLAMP_TO_EDGE 环绕模式
 *
 * 5. 性能问题
 * ---------------------------------------------------------------------------
 *    可能原因:
 *    - 太多 draw call
 *    - 频繁切换程序/状态
 *    - 使用了 DYNAMIC_DRAW 而数据实际是静态的
 *    优化: 批量绘制、减少状态切换、使用 STATIC_DRAW
 *
 * ============================================================================
 * 常量速查表
 * ============================================================================
 *
 * 图元类型:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_POINTS          = 0x0000  点                       │
 * │ GL_LINES           = 0x0001  线段                     │
 * │ GL_LINE_LOOP       = 0x0002  线环                     │
 * │ GL_LINE_STRIP      = 0x0003  线条带                   │
 * │ GL_TRIANGLES       = 0x0004  三角形                   │
 * │ GL_TRIANGLE_STRIP  = 0x0005  三角形条带               │
 * │ GL_TRIANGLE_FAN    = 0x0006  三角形扇                 │
 * └────────────────────────────────────────────────────────┘
 *
 * 缓冲区:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_ARRAY_BUFFER         = 0x8892  顶点数组缓冲区       │
 * │ GL_ELEMENT_ARRAY_BUFFER = 0x8893  索引缓冲区          │
 * │ GL_STATIC_DRAW         = 0x88E4  静态绘制             │
 * │ GL_DYNAMIC_DRAW       = 0x88E8  动态绘制             │
 * └────────────────────────────────────────────────────────┘
 *
 * 启用特性:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_CULL_FACE        = 0x0B44  背面剔除               │
 * │ GL_BLEND            = 0x0BE2  混合                    │
 * │ GL_DEPTH_TEST       = 0x0B71  深度测试                 │
 * │ GL_STENCIL_TEST     = 0x0B90  模板测试                │
 * │ GL_SCISSOR_TEST    = 0x0C11  裁剪测试                │
 * └────────────────────────────────────────────────────────┘
 *
 * 数据类型:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_BYTE           = 0x1400  int8                      │
 * │ GL_UNSIGNED_BYTE  = 0x1401  uint8                     │
 * │ GL_SHORT          = 0x1402  int16                     │
 * │ GL_UNSIGNED_SHORT = 0x1403  uint16                    │
 * │ GL_INT            = 0x1404  int32                     │
 * │ GL_UNSIGNED_INT   = 0x1405  uint32                    │
 * │ GL_FLOAT          = 0x1406  float32                   │
 * └────────────────────────────────────────────────────────┘
 *
 * 纹理格式:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_RGB             = 0x1907  RGB                      │
 * │ GL_RGBA            = 0x1908  RGBA                     │
 * │ GL_DEPTH_COMPONENT = 0x1902  深度                    │
 * └────────────────────────────────────────────────────────┘
 *
 * 着色器常量:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_VERTEX_SHADER   = 0x8B31  顶点着色器               │
 * │ GL_FRAGMENT_SHADER = 0x8B30  片段着色器               │
 * │ GL_LINK_STATUS    = 0x8B82  链接状态                  │
 * └────────────────────────────────────────────────────────┘
 *
 * 比较函数:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_NEVER          = 0x0200  从不通过                  │
 * │ GL_LESS           = 0x0201  小于通过                   │
 * │ GL_EQUAL          = 0x0202  等于通过                   │
 * │ GL_LEQUAL         = 0x0203  小于等于通过               │
 * │ GL_GREATER        = 0x0204  大于通过                  │
 * │ GL_NOTEQUAL       = 0x0205  不等于通过                │
 * │ GL_GEQUAL         = 0x0206  大于等于通过              │
 * │ GL_ALWAYS         = 0x0207  总是通过                  │
 * └────────────────────────────────────────────────────────┘
 *
 * 错误代码:
 * ┌────────────────────────────────────────────────────────┐
 * │ GL_NO_ERROR          = 0      无错误                   │
 * │ GL_INVALID_ENUM      = 0x0500  无效枚举                │
 * │ GL_INVALID_VALUE     = 0x0501  无效值                  │
 * │ GL_INVALID_OPERATION = 0x0502  无效操作                │
 * │ GL_OUT_OF_MEMORY     = 0x0505  内存不足                │
 * └────────────────────────────────────────────────────────┘
 *
 * ============================================================================
 * 学习路径建议
 * ============================================================================
 *
 * 第一阶段：基础入门
 * 1. 理解渲染管线基本概念
 * 2. 掌握着色器编写（GLSL 语法）
 * 3. 实现最简单的三角形渲染
 *
 * 第二阶段：核心功能
 * 4. 顶点缓冲区的创建和使用
 * 5. 纹理加载和采样
 * 6. MVP 矩阵变换
 * 7. 深度测试和背面剔除
 *
 * 第三阶段：进阶特性
 * 8. 混合和透明效果
 * 9. 帧缓冲对象（FBO）
 * 10. 模板缓冲区应用
 * 11. 高级着色器技术
 *
 * 第四阶段：性能优化
 * 12. 批处理渲染
 * 13. 遮挡剔除
 * 14. GPU 纹理压缩
 * 15. 着色器优化
 *
 * ============================================================================
 * 参考资料
 * ============================================================================
 *
 * - OpenGL ES 2.0 规范: https://www.khronos.org/opengles/
 * - GLSL ES 规范: https://www.khronos.org/registry/OpenGL/specs/GLSLangSpec.1.0.33.pdf
 * - Android OpenGL ES 文档: https://developer.android.com/guide/topics/graphics/opengl
 * - learnopengles.com: 非常好的 OpenGL ES 教程
 *
 * ============================================================================
 */
